namespace BokLib.Utils
{
    public class BokModInfo
    {
        public readonly string Name;
        public readonly string Version;

        public BokModInfo(string name, string version)
        {
            Name = name;
            Version = version;
        }
    }
}